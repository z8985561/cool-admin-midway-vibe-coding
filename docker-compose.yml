# 本地数据库环境
# 已改为手动管理 Docker 容器，不再使用此 compose 文件
# 手动 MySQL: docker run -d --name cool-mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=123456 -e MYSQL_DATABASE=cool -v cool-mysql-data:/var/lib/mysql --restart unless-stopped mysql:8.0 --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
# 手动 Redis: docker run -d --name cool-redis -p 6379:6379 -v cool-redis-data:/data --restart unless-stopped redis
version: "3.1"

services:
  # coolDB:
  #   image: mysql
  #   command:
  #     --default-authentication-plugin=mysql_native_password
  #     --sql_mode=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION
  #     --group_concat_max_len=102400
  #   restart: always
  #   volumes:
  #     - ./data/mysql/:/var/lib/mysql/
  #   environment:
  #     TZ: Asia/Shanghai
  #     MYSQL_ROOT_PASSWORD: "123456"
  #     MYSQL_DATABASE: "cool"
  #     MYSQL_USER: "root"
  #     MYSQL_PASSWORD: "123456"
  #   networks:
  #     - cool
  #   ports:
  #     - 3306:3306

  # coolRedis:
  #   image: redis
  #   restart: always
  #   environment:
  #     TZ: Asia/Shanghai
  #     volumes:
  #       - ./data/redis/:/data/
  #     networks:
  #       - cool
  #     ports:
  #       - 6379:6379
